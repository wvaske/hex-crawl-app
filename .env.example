# HexCrawl VTT — configuration contract
#
# Copy to `.env` at the repo root (or next to your docker-compose file) and edit.
# `.env` is gitignored and MUST NEVER be committed: it is where instance-specific
# values live. Every value below is optional — the defaults shown are what the
# app uses when the variable is unset.
#
# Precedence: real environment variables always win over this file. The server
# reads `.env` only to fill in variables that are not already set, so compose
# `environment:`, `docker run -e`, and systemd units override it.
#
# Only CREATE_PASSWORD, DATABASE_URL (and the MCP/wiki values further down) are
# secrets. Per-campaign DM/player join secrets are generated by the app and
# stored in the database — not in config.

# ---------------------------------------------------------------------------
# Server (packages/server/src/config.ts)
# ---------------------------------------------------------------------------

# TCP port the HTTP + WebSocket server listens on.
# Default: 3000
PORT=3000

# Interface to bind. 0.0.0.0 listens on all interfaces (required inside Docker).
# Use 127.0.0.1 to accept only local connections, e.g. behind a host reverse proxy.
# Default: 0.0.0.0
HOST=0.0.0.0

# Directory holding ALL persistent state: the SQLite database (hexcrawl.db) and
# uploaded map images (uploads/). Back up a deployment by copying this directory;
# restore by putting it back. Must be writable by the server process.
# Default: ../../data relative to packages/server (i.e. ./data in a source checkout)
# In the official container image this is /data, backed by a volume.
# Uploaded images live here even when DATABASE_URL moves the rows elsewhere.
DATA_DIR=./data

# Alternate storage backend: a PostgreSQL connection string. Unset — the
# default — keeps the embedded SQLite database in DATA_DIR, which is the
# quickstart path and needs no other moving parts.
#
# Set it when DATA_DIR can only be network storage: SQLite's WAL mode is NOT
# safe on NFS/SMB (locking is advisory there, and the failure mode is silent
# corruption). Postgres has no such constraint.
#
# The server creates and migrates its own schema, so an empty database and a
# user with CREATE TABLE is the whole provisioning story. Uploaded images stay
# on disk under DATA_DIR either way, so that volume is still required and still
# needs backing up. Run ONE server process against a given database — the app
# holds campaign state in memory and writes through to storage.
#
# Migrating an existing instance: export each campaign and import the archives
# into a Postgres-backed instance — see deploy/RUNBOOK.md § 11.
# CONTAINS A SECRET (the database password) when set.
# Default: unset (embedded SQLite under DATA_DIR)
# DATABASE_URL=postgres://hexcrawl:<password>@db.example.com:5432/hexcrawl

# Absolute or relative path to the built client (packages/client/dist). When set
# and it contains index.html, the server also serves the web UI and falls back to
# index.html for deep links. Leave unset in development — Vite serves the client
# on :5173 instead.
# Default: unset (API/WebSocket only)
# In the official container image this is /app/client-dist.
# CLIENT_DIST=./packages/client/dist

# ---------------------------------------------------------------------------
# Public-instance hardening (packages/server/src/http/security.ts)
# ---------------------------------------------------------------------------
# The defaults are sized for a private instance among friends. Tighten them if
# strangers can reach the server. See deploy/RUNBOOK.md § Security.

# Instance-level gate on campaign creation. When set, both "Create campaign" and
# "Restore from backup" require this password (the landing page grows a password
# field), so a public instance is not a free campaign-hosting service.
# IS A SECRET when set — share it only with people allowed to make campaigns.
# Default: unset (anyone who can load the page can create a campaign)
# CREATE_PASSWORD=<choose something long>

# Per-campaign cap on the total size of uploaded map images, in megabytes.
# Uploads that would cross it are rejected with 413. 0 disables the quota.
# Default: 200
# UPLOAD_QUOTA_MB=200

# Per-IP rate limits, in requests per RATE_LIMIT_WINDOW_SEC. Set any to 0 to
# disable that limit. The limiter is in-memory and per-process.
# Defaults: window 60s, create 3, import 3, join 10, export 10
# RATE_LIMIT_WINDOW_SEC=60
# RATE_LIMIT_CREATE=3
# RATE_LIMIT_IMPORT=3
# RATE_LIMIT_JOIN=10
# RATE_LIMIT_EXPORT=10

# Who the rate limiter thinks the client is. Enabled by default, because the
# documented deployment sits behind a reverse proxy and the socket address would
# otherwise be the proxy's (one shared bucket for the whole internet). Set to 0
# when the server is exposed DIRECTLY to clients — otherwise anyone can spoof
# X-Forwarded-For and sail past every limit.
# Default: 1 (trust the first X-Forwarded-For hop)
# TRUST_PROXY=1

# ---------------------------------------------------------------------------
# MCP server (mcp/hexcrawl-mcp.mjs) — optional, only for AI integrations
# ---------------------------------------------------------------------------
# These are read by the standalone MCP process, not by the app server. They are
# usually passed via `claude mcp add -e ...` rather than a .env file. HEXCRAWL_TOKEN
# IS A SECRET: it is a campaign's DM key, which grants full DM access to that
# campaign. Treat it like a password and never commit it.
#
# HEXCRAWL_URL=http://localhost:3000
# HEXCRAWL_CAMPAIGN=<campaign id from the DM link>
# HEXCRAWL_TOKEN=<campaign DM key>

# ---------------------------------------------------------------------------
# Coming soon — not read by any code yet. Documented here so the field names are
# stable and operators can provision them ahead of time.
# ---------------------------------------------------------------------------

# MediaWiki integration: let the server read/write campaign wiki pages directly
# instead of going through the MCP bridge. WIKI_BOT_PASSWORD IS A SECRET
# (MediaWiki Special:BotPasswords grant) — keep it out of the repo and rotate it
# if it is ever exposed.
# WIKI_API_URL=https://wiki.example.com/api.php
# WIKI_BOT_USER=HexCrawlBot@hexcrawl
# WIKI_BOT_PASSWORD=<bot password>

# Canonical externally-visible base URL, used to build join links shown in the
# Setup tab. Unset means links are built from the browser's current origin.
# PUBLIC_URL=https://hexcrawl.example.com
