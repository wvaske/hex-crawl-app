// Re-export shared hex types for client convenience
export type {
  HexCoord,
  CubeCoord,
  TerrainType,
  HexData,
} from '@hex-crawl/shared';

export {
  TERRAIN_TYPES,
  TERRAIN_COLORS,
  hexKey,
  parseHexKey,
} from '@hex-crawl/shared';
